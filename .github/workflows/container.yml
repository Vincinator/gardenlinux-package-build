name: build-pkg-container 
on:
  workflow_call:
  workflow_dispatch:
  schedule:
   - cron: '0 3 * * *'

env:
  IMAGE_NAME: vincinator/package-build
  GARDENLINUX_BUILD_CRE: "docker"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: login to ghcr.io
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u $ --password-stdin

      - name: make package build container
        run: make pkg-build-container

      - name: publish container to ghcr.io
        run: | 
          docker tag "$IMAGE_NAME:today" "ghcr.io/vincinator/$IMAGE_NAME:today"
          docker tag "$IMAGE_NAME:today" "ghcr.io/vincinator/$IMAGE_NAME:$GITHUB_SHA"
          docker push "ghcr.io/vincinator/$IMAGE_NAME:today"
          docker push "ghcr.io/vincinator/$IMAGE_NAME:$GITHUB_SHA"
